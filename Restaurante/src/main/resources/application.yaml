spring:
  application:
    name: Restaurante

  datasource:
    url: jdbc:postgresql://localhost:5432/DBRestaurante
    username: postgres
    password: nueva_password

  jpa:
    show-sql: false
    hibernate:
        ddl-auto: update
    properties:
      hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect